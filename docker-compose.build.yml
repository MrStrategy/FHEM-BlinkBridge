services:
  blink-bridge:
    build: .
    image: fhem-blink-bridge:0.1.0
    container_name: blink-bridge
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./data:/data
